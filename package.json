{
  "name": "ascend-velocity-root",
  "private": true,
  "scripts": {
    "dev": "npm --prefix ascend-velocity run dev",
    "build": "npm --prefix ascend-velocity run build",
    "preview": "npm --prefix ascend-velocity run preview",
    "lint": "npm --prefix ascend-velocity run lint",
    "test": "npm --prefix ascend-velocity run test"
  }
}
